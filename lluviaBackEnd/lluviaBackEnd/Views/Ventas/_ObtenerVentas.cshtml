@using System.Globalization;
@model List<lluviaBackEnd.Models.Ventas>
@{
    /**/

    List<lluviaBackEnd.Models.Ventas> lstVentas = ViewBag.lstVentas;
    DateTime hoy = DateTime.Now;
    lluviaBackEnd.Models.Sesion usuario = Session["UsuarioActual"] as lluviaBackEnd.Models.Sesion;
    ViewBag.devolucionesPermitidas = usuario.devolucionesPermitidas;
    ViewBag.agregarProductosPermitidos = usuario.agregarProductosPermitidos;
    //List<SelectListItem> lstLineasDeProductos = (List<SelectListItem>)ViewBag.lstLineasDeProductos;
    //List<SelectListItem> lstClientes = (List<SelectListItem>)ViewBag.lstClientes;
    //List<SelectListItem> lstUsuarios = (List<SelectListItem>)ViewBag.lstUsuarios;
}



@if (lstVentas.Count <= 0)
{

    <div class="empty-state">
        <div class="empty-state-icon">
            <i class="fas fa-info"></i>
        </div>
        <h2>No existen ventas para el periodo seleccionado.</h2>
    </div>

}
else
{


    <div class="section-body">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped" id="tablaConsultaVentas">
                                <thead>
                                    <tr>
                                        <th class="text-center"> # Ticket </th>
                                        <th>Cliente</th>
                                        <th>Fecha</th>
                                        <th>Monto</th>
                                        <th>Cantidad</th>
                                        <th>Usuario</th>
                                        <th>Forma de pago</th>
                                        <th>Código de Barras</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in lstVentas)
                                    {
                                        string badge = string.Empty;
                                        switch (item.idFactFormaPago)
                                        {
                                            case 1:
                                                badge = "badge badge-primary badge-shadow";

                                                break;
                                            case 2:
                                                badge = "badge badge-info badge-shadow";

                                                break;
                                            case 3:
                                                badge = "badge badge-secondary badge-shadow";

                                                break;
                                            case 4:
                                                badge = "badge badge-dark badge-shadow";

                                                break;
                                            case 18:
                                                badge = "badge badge-blue badge-shadow";

                                                break;
                                            default:
                                                break;
                                        }


                                        if ( item.productosDevueltos > 0) {

                                            lluviaBackEnd.Models.Notificacion<List<lluviaBackEnd.Models.Ticket>> notificacion = new lluviaBackEnd.Models.Notificacion<List<lluviaBackEnd.Models.Ticket>>();
                                            notificacion = new lluviaBackEnd.DAO.VentasDAO().ObtenerTicketsDevolucionComplemento(item.idVenta, lluviaBackEnd.Models.TipoTicket.Devolucion);
                                            item.lstTicketsDevolucion = notificacion.Modelo;

                                        }


                                        if ( item.productosAgregados > 0 )
                                        {

                                            lluviaBackEnd.Models.Notificacion<List<lluviaBackEnd.Models.Ticket>> notificacion = new lluviaBackEnd.Models.Notificacion<List<lluviaBackEnd.Models.Ticket>>();
                                            notificacion = new lluviaBackEnd.DAO.VentasDAO().ObtenerTicketsDevolucionComplemento(item.idVenta, lluviaBackEnd.Models.TipoTicket.Complemento);
                                            item.lstTicketsComplementos = notificacion.Modelo;

                                        }


                                        <tr>
                                            <td>@item.idVenta</td>
                                            <td>@item.nombreCliente</td>
                                            <td>@item.fechaAlta</td>
                                            <td>@item.montoTotal.ToString("C", new CultureInfo("en-US"))</td>
                                            <td>@item.cantidad</td>
                                            <td>@item.nombreUsuario</td>
                                            <td><div class="@badge">@item.descripcionFactFormaPago</div></td>
                                            <td>@item.codigoBarrasTicket</td>
                                            <td>


                                                <div class="dropdown d-inline">
                                                    <button class="btn btn-primary dropdown-toggle" type="button" id="menuAccionesVentas" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                        Acciones
                                                    </button>
                                                    <div class="dropdown-menu">

                                                        @if (item.idEstatusFactura == (int)lluviaBackEnd.Models.EnumEstatusFactura.Facturada)
                                                        {
                                                            <a class="dropdown-item has-icon" href="@item.rutaFactura" target="_blank"><i class="fas fa-eye"></i>Ver Factura</a>
                                                            <a class="dropdown-item has-icon" href="javascript:CancelarFactura(@item.idVenta);"><i class="fas fa-ban"></i>Cancelar Factura</a>
                                                        }

                                                        @if (item.idEstatusFactura == 0 || item.idEstatusFactura == (int)lluviaBackEnd.Models.EnumEstatusFactura.Cancelada)
                                                        {
                                                            <a class="dropdown-item has-icon" href="javascript:modalFacturar(@item.idVenta);"><i class="fas fa-file-invoice-dollar"></i>Generar Factura</a>
                                                            <a class="dropdown-item has-icon" href="javascript:CancelaVenta(@item.idVenta);"><i class="fas fa-times"></i>Cancelar Venta</a>
                                                        }

                                                        <a class="dropdown-item has-icon" href="javascript:ImprimeTicket(@item.idVenta);"><i class="fas fa-print"></i>Imprimir Ticket</a>
                                                        <a class="dropdown-item has-icon" href="@Url.Action("VerTicket", "Ventas", new {idVenta=item.idVenta})" target="_blank"><i class="fas fa-eye"></i>Ver Ticket</a>

                                                        @if (item.cantProductosLiq > 0)
                                                        {
                                                            <a class="dropdown-item has-icon" href="javascript:ImprimeTicketDespachadores(@item.idVenta);"><i class="fas fa-print"></i>Ticket Despachadores</a>
                                                            <a class="dropdown-item has-icon" href="@Url.Action("VerTicketDespachadores", "Ventas", new {idVenta=item.idVenta})" target="_blank"><i class="fas fa-eye"></i>Ticket Despachadores</a>
                                                        }


                                                        @if (item.productosAgregados > 0)
                                                        {
                                                            foreach (var item_ in item.lstTicketsComplementos)

                                                            {
                                                                <a class="dropdown-item has-icon" href="javascript:ImprimeTicketComplemento(@item.idVenta, @item_.idComplemento);"><i class="fas fa-print"></i>Imp. Ticket Comp. - @item_.idComplemento</a>
                                                                <a class="dropdown-item has-icon" href="@Url.Action("VerTicketComplemento", "Ventas", new {idVenta = item.idVenta, idComplemento=item_.idComplemento})" target="_blank"><i class="fas fa-eye"></i>Ver Ticket Comp. - @item_.idComplemento  </a>

                                                            }
                                                        }



                                                        @*@if (item.productosDevueltos > 0)
                                                        {
                                                            <a class="dropdown-item has-icon" href="javascript:ImprimeTicketDevolucion(@item.idVenta);"><i class="fas fa-print"></i>Imprimir Ticket Devolución</a>
                                                            <a class="dropdown-item has-icon" href="@Url.Action("VerTicketDevolucion", "Ventas", new {idVenta=item.idVenta})" target="_blank"><i class="fas fa-eye"></i>Ver Ticket de Devolución  </a>
                                                        }*@


                                                        @if (item.productosDevueltos > 0)
                                                        {
                                                            foreach (var item_ in item.lstTicketsDevolucion)

                                                            {
                                                                <a class="dropdown-item has-icon" href="javascript:ImprimeTicketDevolucion(@item.idVenta, @item_.idDevolucion);"><i class="fas fa-print"></i>Imp. Ticket Dev. - @item_.idDevolucion</a>
                                                                <a class="dropdown-item has-icon" href="@Url.Action("VerTicketDevolucion", "Ventas", new {idVenta = item.idVenta, idDevolucion=item_.idDevolucion})" target="_blank"><i class="fas fa-eye"></i>Ver Ticket Dev. - @item_.idDevolucion  </a>

                                                            }
                                                        }


                                                        @*@if (item.productosDevueltos > 0)
                                                        {
                                                            foreach (var item_ in item.lstTicketsDevolucion)

                                                            {
                                                                <a class="dropdown-item has-icon" href="javascript:ImprimeTicketDevolucion(@item.idVenta, @item_.idDevolucion);"><i class="fas fa-print"></i>Ticket Devolución - @item_.idDevolucion</a>
                                                                <a class="dropdown-item has-icon" href="@Url.Action("VerTicketDevolucion", "Ventas", new {idVenta = item.idVenta, idDevolucion=item_.idDevolucion})" target="_blank"><i class="fas fa-eye"></i>Ver Ticket de Dev. - @item_.idDevolucion  </a>

                                                            }
                                                        }*@





                                                        @if ((item.productosDevueltos < ViewBag.devolucionesPermitidas) && (item.fechaAlta.Date == hoy.Date) && (item.idEstatusFactura != 1))
                                                        {
                                                            <a class="dropdown-item has-icon" href="@Url.Action("Ventas", "Ventas", new { idVenta = item.idVenta, esDevolucion = "true", esAgregarProductos = "false" })"><i class="far fa-minus-square"></i>Devolver Productos</a>
                                                        }

                                                        @if ((item.productosAgregados < ViewBag.agregarProductosPermitidos) && (item.cantidad >= 6) && (item.fechaAlta.Date == hoy.Date) && (item.idEstatusFactura != 1))
                                                        {
                                                            <a class="dropdown-item has-icon" href="@Url.Action("Ventas", "Ventas", new { idVenta = item.idVenta, esAgregarProductos = "true", esDevolucion = "false" })"><i class="far fa-plus-square"></i>Agregar Productos</a>
                                                        }

                                                    </div>
                                                </div>

                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>




}




